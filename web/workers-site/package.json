{
  "private": true,
  "name": "img2pic-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers for img2pic web application",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/kv-asset-handler": "~0.3.0",
    "wrangler": "^3.0.0"
  }
}